defmodule Foo do
  @moduledoc """
  foobar
  bar foo
  """
  def one() do
    "one"
  end

  def foo(one, two) do
    
  end

  def far(bar, zar) do
    ""
  end

  def two(one, two) do
    "#{one} #{two}"
  end
end
