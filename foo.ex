defmodule Foo do
  def one() do
  end

  def foo(one, two) do
  end

  def far(bar, zar) do
    ""
  end

  [
    foo: :bar,
    bar: :foo
  ]

  def twosadfthree(
        pone,
        two
      ) do
    "#{one} #{two}"
  end
end

defmodule Foo do
  defmodule Foo do
  end
end
